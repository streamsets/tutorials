apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sdc-stage-libs-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: sdc-stage-libs-sc
  resources:
    requests:
      storage: 5Gi
